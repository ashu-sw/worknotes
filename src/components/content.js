import React from 'react';

const Content = (props) => {
    const welcomeMsg = 'Welcome to work Notes';
    return (
        <div>
            <h2>{welcomeMsg}</h2>
        </div>
    )
}

export default Content;

